{
  "name": "@dokdo-academy/common",
  "packageManager": "yarn@3.4.1",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf -rf dist && rm tsconfig.tsbuildinfo",
    "compile": "tsc"
  },
  "devDependencies": {
    "rimraf": "^4.3.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.13"
  }
}
